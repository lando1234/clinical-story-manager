#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Ejecutar lint-staged (solo archivos modificados)
npx lint-staged

# Ejecutar tests (todo el proyecto)
npm run pre-commit:test

# Ejecutar build completo (simula Vercel)
npm run pre-commit:build

